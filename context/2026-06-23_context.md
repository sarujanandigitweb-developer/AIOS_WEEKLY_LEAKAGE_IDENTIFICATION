# WLSP Day Context — 2026-06-23 (D02)

**Deliverable:** D02 · **Developer:** sarujanan · **Phase:** Source validation + calculation design
**Activity memory:** `daily_task.tbl_wlsp_sarujanan` rows **D02-A11 … D02-A20**

---

## D02 Source Validation
Profiled the four source tables live: `ppc_performance`, `order_transaction`, `order_shipping_billing_detail`, `amazon_returns`. **Readiness ~85–95%**; all five analyses calculable directly from source.
- Shipping coverage **99.6–99.7% on completed UK FBM** orders (the raw 35.7%-null figure is across all statuses).
- PPC PH attribution **40.5% null** (DQ-1); order PH-null ~20% (30d).
- Refund sources disagree (DQ-2); `amazon_returns.refunded_amount` ~41% null.

## L1–L5 Coverage Audit
- **0** analyses reusable as-is; **L2** reusable with **minor modification** (existing `SHIPPING_HIGH` already uses the 25% threshold); **L1, L3, L4, L5** require **new** calculations.
- The `development.leakage_*` tables are a **derived summary + lifecycle layer**, not a calculation source — they hold no PPC, raw revenue, raw shipping, or refund-count fields.

## Framework Comparison (direct source vs existing)
- **L1:** existing `PAID_MEDIA_BLEED` = ACOS-ratio 30d (2 rows) ≠ protocol zero-conversion 7d.
- **L2:** `SHIPPING_HIGH` 148 rows (30d) — same 25% threshold, different window/grain.
- **L3:** existing net-negative 404 rows excludes PPC; **0 overlap** with protocol L3 (7d + PPC>£5).
- **L4:** `RETURNS_ISSUE` = **0 rows** (never implemented).
- **L5:** no PH-month grain exists.

## Source-Table Findings
- `ref_id` = ASIN; `record_type='ad'` grain.
- Shipping is per `order_id` → must **de-duplicate per order_id** and allocate by revenue share before any ASIN ratio.
- `ppc_performance` must be **window-scoped** (24M rows; unscoped scans time out).
- Use `order_transaction` as the **single L4 rate source**; `amazon_returns` for £/reason enrichment only.

## Decisions
- Applied audit corrections **C-1…C-7** → `VALIDATION_AUDIT` re-graded to clean **PASS**.
- Architecture posture: **hybrid** — render the report from source on demand, persist only the flagged set through the existing lifecycle.
- **Reuse the existing PH closure lifecycle** (`development.actions` + `ph_action_*`, 11-state taxonomy, verification schedule) — **no new tracking/storage tables**.
- New rule **WLSP-VAL-02**: every import CSV row must have exactly **24 columns** (per-row check, not header-only).

## Next Step
Build the dashboard directly from source (D03) once OPTION B is signed off.

---
*Activity-memory link: rows D02-A11…D02-A20 in `daily_task.tbl_wlsp_sarujanan`.*
